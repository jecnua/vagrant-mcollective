#!/bin/bash

# if [ -f /opt/activem_installed ]
# then
#   wget http://apt.puppetlabs.com/puppetlabs-release-trusty.deb
#   dpkg -i puppetlabs-release-trusty.deb
#   apt-get update
#
#   apt-get install -y activemq
#   update-rc.d activemq multiuser
# fi
